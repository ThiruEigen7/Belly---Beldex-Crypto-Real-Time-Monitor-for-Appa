# 🌐 BELLY - Cloud Deployment Architecture

## 📊 System Architecture (Cloud)

```
┌─────────────────────────────────────────────────────────────┐
│                   INTERNET (EVERYONE)                        │
│              https://your-domain.com                         │
└────────────┬──────────────────────────────────────────────────┘
             │
             ▼
        ┌────────────┐
        │   CDN/SSL  │  (CloudFlare / Railway)
        └─────┬──────┘  - HTTPS enabled
              │         - DDoS protection
              ▼
    ┌─────────────────────────────┐
    │    Load Balancer            │  (Railway/Vercel/GCP)
    │   (Routes to services)      │  - Auto-scaling
    └──┬──────────────────────┬───┘  - SSL termination
       │                      │
       ▼                      ▼
   ┌─────────────┐      ┌──────────────┐
   │  Frontend   │      │   Backend    │
   │  (Reflex)   │      │  (FastAPI)   │
   │  Port 3000  │      │  Port 8000   │
   └─────┬───────┘      └──────┬───────┘
         │                     │
         │                     ▼
         │            ┌────────────────────┐
         │            │   API Services     │
         │            │ - Current Price    │
         │            │ - History          │
         │            │ - Stats            │
         │            │ - Predictions      │
         │            │ - Health Check     │
         │            └────────┬───────────┘
         │                     │
         └──────┬──────────────┘
                │
    ┌───────────┴────────────────────────────┐
    │                                         │
    ▼                                         ▼
┌──────────────┐                   ┌──────────────────┐
│  Supabase    │                   │  Redis Upstash   │
│ PostgreSQL   │                   │  (Cache Layer)   │
│              │                   │                  │
│ - Prices     │                   │ - Hot Cache      │
│ - Stats      │                   │ - Sessions       │
│ - Predictions│                   │ - Real-time Data │
└──────────────┘                   └──────────────────┘
       ▲                                    ▲
       │                                    │
    ┌──┴────────────────────────────────────┤
    │                                        │
    ▼                                        ▼
 ┌─────────────────────────────────────────────┐
 │         Background Workers                  │
 │                                             │
 │  ┌─────────────────────────────────────┐  │
 │  │  Kafka Producer                     │  │
 │  │  - Fetches prices from CoinGecko    │  │
 │  │  - Runs every 10 minutes            │  │
 │  │  - Adds ₹0.10 markup                │  │
 │  └────────────────────────┬────────────┘  │
 │                           │                │
 │  ┌────────────────────────▼────────────┐  │
 │  │  Kafka / Redpanda Cloud             │  │
 │  │  - Message broker                   │  │
 │  │  - Cloud-hosted                     │  │
 │  └────────────────────────┬────────────┘  │
 │                           │                │
 │  ┌────────────────────────▼────────────┐  │
 │  │  Kafka Consumer                     │  │
 │  │  - Consumes price messages          │  │
 │  │  - Writes to Redis                  │  │
 │  │  - Writes to Supabase               │  │
 │  └─────────────────────────────────────┘  │
 │                                             │
 │  ┌─────────────────────────────────────┐  │
 │  │  Airflow DAGs                       │  │
 │  │  - stats_computation_24h            │  │
 │  │  - price_prediction_nightly         │  │
 │  └─────────────────────────────────────┘  │
 └─────────────────────────────────────────────┘
```

---

## 🚀 Deployment Topology

```
Your GitHub Repository
         │
         ▼
    ┌─────────────────┐
    │  GitHub Hooks   │  (Auto-trigger on push)
    └────────┬────────┘
             │
    ┌────────┴─────────────────────────────┐
    │                                       │
    ▼                                       ▼
┌─────────────┐                   ┌─────────────────┐
│   Railway   │                   │     Vercel      │
│ (Backend)   │                   │  (Frontend)     │
│ - Auto-     │                   │ - Auto-deploys  │
│   deploys   │                   │ - CDN hosting   │
│ - Monitors  │                   │ - Edge functions│
│ - Logs      │                   │                 │
└─────────────┘                   └─────────────────┘
```

---

## 🔄 Data Flow (Per Request)

```
User Browser Request
        │
        ▼
   HTTPS/SSL
   (Railway)
        │
        ▼
  Load Balancer
        │
    ┌───┴──────────┐
    │              │
    ▼              ▼
Frontend API      Backend API
(Reflex)          (FastAPI)
    │              │
    ├──────────────┤
    │              │
    ▼ (fetch)      ▼ (endpoint)
  API Call    Processing
    │              │
    ▼              ▼
Redis Cache    Database Check
    │              │
    └──────┬───────┘
           │
        Cache Hit?
        /      \
      Yes      No
      │        │
      ▼        ▼
  Return    Query DB
  Fast      (Slower)
           │
           ▼
      Cache Result
           │
           ▼
      Return to User
```

---

## 📦 Service Deployment Map

### Option A: Single Container (Simple)
```
┌──────────────────────────────┐
│   Railway Container          │
│                              │
│  FastAPI Backend             │
│  + All API endpoints         │
│  + Health checks             │
│  + Auto-restart              │
│  + Auto-scale                │
│                              │
│  Port: 8000                  │
└──────────────────────────────┘
         ↓
    Supabase + Redis
```

### Option B: Multi-Container (Production)
```
┌─────────────────────────────────────────┐
│      Docker Compose / Kubernetes        │
│                                         │
│  ┌─────────┐  ┌─────────┐  ┌────────┐ │
│  │Frontend │  │ Backend │  │Producer│ │
│  │Reflex   │  │FastAPI  │  │Kafka   │ │
│  └─────────┘  └─────────┘  └────────┘ │
│                                         │
│  ┌─────────┐  ┌─────────┐  ┌────────┐ │
│  │Consumer │  │Airflow  │  │ Redis  │ │
│  │Kafka    │  │Scheduler│  │Local   │ │
│  └─────────┘  └─────────┘  └────────┘ │
│                                         │
│  Networking: Internal Docker network   │
│  Storage: Persistent volumes           │
│  Monitoring: Built-in                  │
└─────────────────────────────────────────┘
```

---

## 🌍 Global Deployment Options

### Tier 1: Single Region (Cheapest)
```
┌─────────────────────────────┐
│   Railway (Single Region)   │
│   - us-central1             │
│   - All services in one zone│
│   - $20-50/month            │
└─────────────────────────────┘
```

### Tier 2: Multi-Region (Better Performance)
```
┌──────────────────┐  ┌──────────────────┐
│  Railway Primary │  │  Railway Backup  │
│  - us-central1   │  │  - eu-west1      │
│  - Full stack    │  │  - Replica DB    │
└──────────────────┘  └──────────────────┘
        │                     │
        └──────────┬──────────┘
                   │
            Global Load Balancer
                   │
                Users
```

### Tier 3: CDN Distribution (Fastest)
```
┌─────────────────────────────┐
│      CloudFlare CDN         │
│  (Caches Frontend Assets)   │
└────────────┬────────────────┘
             │
    ┌────────┴──────────┐
    │                   │
┌───▼────────┐  ┌──────▼────┐
│Backend API │  │  Cached   │
│Railway     │  │  Content  │
└────────────┘  └───────────┘
```

---

## 💰 Cost Breakdown

### Railway Single Container
```
┌─────────────────────────────────┐
│  Cost per Month                 │
├─────────────────────────────────┤
│ Railway $20-50                  │
│ Supabase $0-25                  │
│ Redis $0-10                     │
│ Kafka $0-20 (cloud-hosted)      │
├─────────────────────────────────┤
│ TOTAL: $20-105/month            │
└─────────────────────────────────┘
```

### Multi-Container Production
```
┌─────────────────────────────────┐
│  Cost per Month                 │
├─────────────────────────────────┤
│ Railway $50-100 (scaled)        │
│ Supabase Pro $25                │
│ Redis Upstash Pro $25           │
│ Kafka Cloud Pro $50             │
│ Monitoring/Backups $25          │
├─────────────────────────────────┤
│ TOTAL: $175-225/month           │
└─────────────────────────────────┘
```

---

## ⚙️ Configuration Layers

```
┌─────────────────────────────────────┐
│   Environment Variables             │
│   (Platform Dashboard)              │
│   - SUPABASE_URL                    │
│   - REDIS_TOKEN                     │
│   - KAFKA_PASSWORD                  │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│   Docker Runtime                    │
│   (Dockerfile)                      │
│   - Base image: python:3.13         │
│   - Dependencies: requirements.txt  │
│   - Startup command                 │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│   Application Configuration         │
│   (Code)                            │
│   - API routes                      │
│   - Database connections            │
│   - Cache strategies                │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│   External Services                 │
│   - Supabase (DB)                   │
│   - Redis (Cache)                   │
│   - Kafka (Streaming)               │
│   - CoinGecko (Price API)           │
└─────────────────────────────────────┘
```

---

## 🔐 Security Layers

```
┌───────────────────────────────────────┐
│   Users on Internet                   │
└────────────┬────────────────────────────┘
             │
             ▼
┌───────────────────────────────────────┐
│   CloudFlare / SSL Certificate        │
│   - HTTPS encryption                  │
│   - DDoS protection                   │
│   - IP rate limiting                  │
└────────────┬────────────────────────────┘
             │
             ▼
┌───────────────────────────────────────┐
│   Railway / Platform Firewall         │
│   - VPC networking                    │
│   - Environment variable encryption   │
│   - Secret rotation                   │
└────────────┬────────────────────────────┘
             │
             ▼
┌───────────────────────────────────────┐
│   Application Layer                   │
│   - CORS validation                   │
│   - API key verification              │
│   - Input validation                  │
│   - Rate limiting                     │
└────────────┬────────────────────────────┘
             │
             ▼
┌───────────────────────────────────────┐
│   Database Layer                      │
│   - Supabase RLS policies             │
│   - Row-level security                │
│   - Encrypted passwords               │
│   - Backup encryption                 │
└───────────────────────────────────────┘
```

---

## 📊 Scaling Architecture

### Level 1: Single Container (0-1000 users)
```
┌──────────────┐
│Railway       │
│Container     │
│- 0.5 CPU     │
│- 512 MB RAM  │
│- Simple      │
│- Cheap       │
└──────────────┘
```

### Level 2: Multiple Replicas (1000-10000 users)
```
┌──────────────┐
│  Railway     │ <- Load Balancer
│ Container 1  │
└──────────────┘
       ↑
       │
┌──────────────┐
│  Railway     │
│ Container 2  │
└──────────────┘
       ↑
       │
┌──────────────┐
│  Railway     │
│ Container 3  │
└──────────────┘
```

### Level 3: Full Kubernetes (10000+ users)
```
┌────────────────────────────────────┐
│  Google Cloud Run / AWS EKS        │
│                                    │
│  Auto-scaling pods                 │
│  Regional distribution             │
│  CDN for static assets             │
│  Managed databases                 │
│  Redis cluster                     │
│  Kafka cluster                     │
└────────────────────────────────────┘
```

---

## 🔄 Deployment Workflow

```
Step 1: Prepare
┌──────────────────────────────┐
│ git add -A                   │
│ git commit -m "Ready"        │
│ git push origin main         │
└──────────────────────────────┘
           │
           ▼
Step 2: Platform Triggered
┌──────────────────────────────┐
│ GitHub → Railway Webhook     │
│ GitHub → Vercel Webhook      │
└──────────────────────────────┘
           │
           ▼
Step 3: Build
┌──────────────────────────────┐
│ Build Docker image           │
│ Run tests (optional)         │
│ Push to registry             │
└──────────────────────────────┘
           │
           ▼
Step 4: Deploy
┌──────────────────────────────┐
│ Stop old containers          │
│ Start new containers         │
│ Health check                 │
│ Roll back if failed          │
└──────────────────────────────┘
           │
           ▼
Step 5: Live
┌──────────────────────────────┐
│ https://your-domain.com      │
│ Live and serving users!      │
└──────────────────────────────┘
```

---

## 📱 User Access Paths

```
Desktop Browser
         │
    ┌────┴─────────────┐
    │                  │
    ▼                  ▼
Chrome Safari → https://your-domain.com
Firefox Edge        │
         ▲           ▼
         │    ┌─────────────────┐
         │    │  CDN / Railway  │
         │    └────────┬────────┘
         │             │
Mobile Browser  ┌──────┴──────┐
(iOS/Android)   │             │
    │           ▼             ▼
    │       Frontend       Backend API
    │       (UI)           (Data)
    └───────→│←────────────┘
             │
         Display Data
         to User
```

---

## 🎯 Quick Reference

### Files You Need
```
✅ Dockerfile              - Container build
✅ docker-compose.prod.yml - Multi-container
✅ deploy_cloud.sh         - Platform chooser
✅ deploy_railway.sh       - Railway automation
✅ DEPLOYMENT_GUIDE.md     - Full guide
✅ CLOUD_ENV_VARIABLES.md  - Variables reference
```

### Commands You Need
```
# Deploy to Railway
git push origin main
# Auto-deploys! ✅

# Deploy locally for testing
docker-compose -f docker-compose.prod.yml up -d

# View logs
docker-compose -f docker-compose.prod.yml logs -f

# Test API
curl https://your-domain.com/health
```

### URLs You Need
```
🌐 Your App: https://your-domain.com
📊 API Docs: https://your-domain.com/docs
⚙️ Admin: https://your-domain.com/admin
📈 Dashboard: Platform-specific dashboard URL
```

---

## ✨ Next Steps

1. **Choose platform** (Railway recommended)
2. **Push code**: `git push origin main`
3. **Set variables**: In platform dashboard
4. **Deploy**: Platform auto-deploys
5. **Test**: `curl https://your-domain.com/health`
6. **Share**: Send URL to users

---

*Architecture Diagram - BELLY Cloud Deployment*
*December 14, 2025*
